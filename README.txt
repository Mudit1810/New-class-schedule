# Class Schedule Application

This application uses relative paths throughout for maximum portability. When cloning from GitHub, you should be able to run it directly without path modifications.

## Running the Application

Use the included start script:
```
./start_server.sh
```

Or run manually:
```
cd PROJECT
python webapp/app.py
```

## File Structure

All internal imports and file paths are relative to maintain portability.
